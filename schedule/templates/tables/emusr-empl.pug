
h3.text-lg.bg-zinc-200.text-black #{emp.name} Unfavorables 

.n #{slots.count}
table  
    tr
        th Date
        th Shift 
        th Actions 
    tbody 
        if slots
            each unf in slots
                tr 
                    td #{unf.workday.date}
                    td #{unf.shift}
                    td 
                        a(href='/wday/#{unf.workday.slug}/') View
                        a(href='') Edit 
                            i.fa-duotone.fa-trashcan(class="m-1 text-gray-500 hover:text-red-500")

  